export function loginWithUsername(credential) {
  return new Promise((resolve, reject) => {
    resolve({});
  });
}

export function checkLogin() {
  return new Promise((resolve, reject) => {
    resolve({});
  });
}

/**
 * Logout from firebase authentication
 * @returns
 */
export function userLogout() {
  return new Promise((resolve, reject) => {
    resolve({});
  });
}
